<h1><%= @collection.title%> <span>> Edit</span></h1>
<%= form_tag("update", :method => "post") do %>

<h2><%= @document.data_template.title%></h2>
<%   @document.data_template.data_template_fields.each do |field| %>
	<%= send(field.input_type+'_form', @document[field.input_name], field) %>
<% end %>
<%= hidden_field_tag 'data_template_id', @document.data_template_id %>
<%= hidden_field_tag 'data_collection_ids[]', @document.data_collection_ids %>
<div>
	<%= submit_tag("Edit Document") %>
</div>
<% end %>

